
# Finlytics AI (Demo)

This is a basic demo of a Streamlit app that:
- Uploads a financial PDF
- Extracts text using pdfplumber
- Allows mock question answering (hardcoded responses)

To run locally:
```bash
pip install -r requirements.txt
streamlit run app.py
```
